name: Trigger Inferno-Docker Build

on:
  push:
    paths:
      - 'src/**'
    branches:
      - main

jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
      - name: Send repository dispatch event to inferno-docker (Private Repo)
        run: |
          curl -X POST -H "Accept: application/vnd.github.v3+json" \
               -H "Authorization: token ${{ secrets.REPO_ACCESS_TOKEN }}" \
               https://api.github.com/repos/bbc8282/inferno-docker/dispatches \
               -d '{"event_type": "inferno_updated"}'
